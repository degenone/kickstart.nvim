-- Plugin specifications are defined in individual files in this directory
-- and are loaded via { import = 'plugins' } in init.lua
-- This file can be left empty as each plugin gets its own file.
return {}
